# Dépendances d'exécution. Installation :
#   python3.13 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# Django 5.2 est une version LTS, supportée jusqu'en avril 2028.
Django==5.2.17
django-environ==0.14.0

# Applications tierces
django-ckeditor==6.7.3
django-honeypot==1.3.0
django-simple-captcha==0.7.0

# Images, paiement, service des fichiers statiques, serveur WSGI
pillow==12.3.0
stripe==15.5.0
whitenoise==6.12.0
gunicorn==26.0.0
